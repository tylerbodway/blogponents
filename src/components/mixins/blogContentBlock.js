export const alignmentProp = {
  props: {
    alignment: {
      type: String,
      required: true
    }
  }
}

export const imageProps = {
  props: {
    image: {
      type: String
    },
    alt: {
      type: String
    }
  }
}